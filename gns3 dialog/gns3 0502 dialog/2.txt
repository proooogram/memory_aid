vmnetX开2到19   拓扑图上用vmnet1 ；  而GNS3 VM改为用vmnet2  减少相互影响的特例
这样 对：
交换机接两个服务器的拓扑图 
S1上如果配置了 ip 192.168.72.44  也不能ping通 192.168.72.1:vmnet1 和 192.168.72.128:GNS3VM  因为此时GNS3VM直接接的是vmnet2:192.168.133.1（不是vmnet1:*72*）


虚拟机S1 vmware的配置接在vmnet1上  但是拓扑图上接在 交换机上 。  为何 S1配置了 vmnet1:*72*的ip后却ping不通72?  难道这正是合理的场景么?（有点等同于真实交换机的情况了）
